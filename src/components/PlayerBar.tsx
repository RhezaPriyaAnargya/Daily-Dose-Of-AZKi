import React from 'react';

const PlayerBar: React.FC = () => (
  <footer className="player-bar">
    {/* Sticky player bar content will go here */}
    <div>Player Bar</div>
  </footer>
);

export default PlayerBar;
